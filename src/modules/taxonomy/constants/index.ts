import { Folder, Tag, Award, Layers, Bookmark, ShoppingBag, Sliders, FileText } from "lucide-react";

export const TAXONOMY_STORAGE_KEY_PREFIX = "axevora_taxonomy_items_";

export const TAXONOMY_ICONS_MAP: Record<string, any> = {
  Folder,
  Tag,
  Award,
  Layers,
  Bookmark,
  ShoppingBag,
  Sliders,
  FileText
};
