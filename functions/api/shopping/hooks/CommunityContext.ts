export class CommunityContext {
  static async getContext(query: string): Promise<string> {
    // Future: Query D1 for top discussions, reviews, expert answers
    return "Community prefers the M3 chip for future-proofing and battery life.";
  }
}
